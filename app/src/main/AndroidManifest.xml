<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appvacov">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppVaCov"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".ActualizarInventario" />
        <activity android:name=".VerInventario" />
        <activity android:name=".ConsultaFase" />
        <activity android:name=".ConsultaSegundaDosis" />
        <activity android:name=".ConsultaVacunadosP" />
        <activity android:name=".ReporteVacunacion" />
        <activity android:name=".ConsultaVacunados" />
        <activity android:name=".AsignarVacuna" />
        <activity android:name=".AgregarVacunas" />
        <activity android:name=".Citas" />
        <activity android:name=".Cita" />
        <activity android:name=".AgendamientoCitas" />
        <activity android:name=".AdminVacunas" />
        <activity android:name=".RegistroAdminVacunas" />
        <activity android:name=".RegistroRepresentanteSitioVacunacion" />
        <activity android:name=".RepresentanteSitioVacunacion" />
        <activity android:name=".UsuarioVacunacion" />
        <activity android:name=".RegistroPersonalVacunacion" />
        <activity android:name=".PersonalVacunacion" />
        <activity android:name=".RegistroUsuario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>